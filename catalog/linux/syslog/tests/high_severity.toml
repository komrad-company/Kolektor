[[tests]]
name = "Syslog — critical severity (emerg=0)"

[[tests.inputs]]
insert_at = "syslog_parse_and_normalize"
type      = "log"

  [tests.inputs.log_fields]
  message   = "Kernel panic - not syncing: Fatal exception"
  timestamp = "2024-04-14T03:15:22Z"
  hostname  = "db01"
  appname   = "kernel"
  facility  = "kern"
  severity  = "emerg"

[[tests.outputs]]
extract_from = "syslog_parse_and_normalize"

[[tests.outputs.conditions]]
type   = "vrl"
source = '''
  assert_eq!(.class_uid, 0)
  assert_eq!(.category_uid, 0)
  assert_eq!(.severity_id, 5)
  assert_eq!(.parse_status, "parsed")
  assert!(contains(string!(.raw), "kernel"))
  assert!(contains(string!(.raw), "Kernel panic - not syncing: Fatal exception"))
'''
