# Hadolint rules intentionally ignored — each exception is justified.
# NB: DL3005 (no apt-get upgrade) was removed from hadolint v2; the upgrade
# in the Dockerfile remains a deliberate choice to patch OS CVEs on a base
# image that is not rebuilt on every Debian advisory.
ignored:
  - DL3008
  # DL3008: apt-get install without pinned versions. Pinning curl to a Debian
  # point-release version breaks on every base refresh; the digest-pinned base
  # plus the downstream grype scan provide the reproducibility guarantee.
