# Hadolint rules intentionally ignored — each exception is justified.
ignore:
  - DL3005
  # DL3005: apt-get upgrade is normally forbidden as non-reproducible.
  # Exception: the base image timberio/vector:0.54.0-debian is not rebuilt
  # on every Debian CVE. We upgrade intentionally to patch OS-level vulnerabilities.
  # A downstream grype scan verifies that no HIGH+ fixable CVE remains.
